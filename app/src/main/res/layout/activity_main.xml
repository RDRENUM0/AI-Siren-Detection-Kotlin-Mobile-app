<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_idle"
    android:padding="16dp">

    <!-- 1. STATUS CARD -->
    <androidx.cardview.widget.CardView
        android:id="@+id/statusCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/card_bg"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="8dp">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center_vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="Monitoring nieaktywny"
            android:textColor="@color/status_text_idle"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium" />
    </androidx.cardview.widget.CardView>

    <!-- 2. SPEKTRALNY WIDOK (FFT) – w CardView z cieniem i zaokrąglonymi rogami -->
    <androidx.cardview.widget.CardView
        android:id="@+id/fftCard"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/statusCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintDimensionRatio="4:3">

        <com.example.sirenfinal.SpectrumView
            android:id="@+id/spectrumView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/fft_bg" />
    </androidx.cardview.widget.CardView>

    <!-- 3. POZIOM GŁOŚNOŚCI (VolumeMeter) – w CardView z cieniem -->
    <androidx.cardview.widget.CardView
        android:id="@+id/meterCard"
        android:layout_width="0dp"
        android:layout_height="48dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/fftCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp">

        <com.example.sirenfinal.VolumeMeterView
            android:id="@+id/volumeMeter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/meter_bg" />
    </androidx.cardview.widget.CardView>

    <!-- 4. Separator -->
    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray"
        app:layout_constraintTop_toBottomOf="@id/meterCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp" />

    <!-- 5. Pole progu -->
    <TextView
        android:id="@+id/thresholdLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Próg:"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:fontFamily="sans-serif"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/thresholdEdit"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/edittext_rounded"
        android:hint="0.7"
        android:text="0.7"
        android:textColor="@android:color/white"
        android:inputType="numberDecimal"
        android:gravity="center_vertical|start"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:fontFamily="sans-serif"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintStart_toEndOf="@id/thresholdLabel"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="12dp" />

    <!-- 6. Drugi separator -->
    <View
        android:id="@+id/divider2"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray"
        app:layout_constraintTop_toBottomOf="@id/thresholdEdit"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp" />

    <!-- 7. PRZYCISKI START / STOP (poziomo obok siebie, 48% szerokości każdy) -->
    <Button
        android:id="@+id/startButton"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_rounded"
        android:backgroundTint="@color/button_start_bg"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:text="START"
        android:textColor="@color/button_text"
        android:textAllCaps="true"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toBottomOf="@id/divider2"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="8dp"
        app:layout_constraintEnd_toStartOf="@id/stopButton"
        android:layout_marginEnd="8dp"
        app:layout_constraintWidth_percent="0.48" />

    <Button
        android:id="@+id/stopButton"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/button_rounded"
        android:backgroundTint="@color/button_stop_bg"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:text="STOP"
        android:textColor="@color/button_text"
        android:textAllCaps="true"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toBottomOf="@id/divider2"
        app:layout_constraintStart_toEndOf="@id/startButton"
        android:layout_marginStart="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="8dp"
        app:layout_constraintWidth_percent="0.48" />

</androidx.constraintlayout.widget.ConstraintLayout>
